$kind: http-request
url: "{{baseUrl}}/avarias"
method: POST
headers:
  Authorization: Bearer {{jwtToken}}
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "descricao": "Avaria Postman",
      "severidade": "media",
      "estado": "pendente",
      "id_lampada": 1
    }
scripts:
  - type: afterResponse
    code: pm.test('Status 201', function () { pm.response.to.have.status(201); });
    language: text/javascript
order: 1000
