$kind: http-request
url: "{{baseUrl}}/sensores-movimento/999999"
method: DELETE
headers:
  Authorization: Bearer {{jwtToken}}
scripts:
  - type: afterResponse
    code: pm.test('Status 404', function () { pm.response.to.have.status(404); });
    language: text/javascript
order: 5000
